{
  "name": "ecommerce-be",
  "version": "1.0.0",
  "description": "API service for the e-commerce app",
  "license": "MIT",
  "main": "build/app.ts",
  "scripts": {
    "build": "set NODE_ENV=production && NODE_OPTIONS=--max-old-space-size=4096 tsc",
    "start": "set NODE_ENV=development && nodemon",
    "inspect": "nodemon --inspect src/app.ts"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "nodemon": "^3.1.1",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.4.5"
  }
}
